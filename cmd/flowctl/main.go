// Command flowctl is the Flow developer/admin CLI. It speaks only to the
// central service (flowd).
//
// Vaihe 0: placeholder so `go build ./...` covers the binary. The `status`
// command lands in Vaihe 1.
package main

import "fmt"

func main() {
	fmt.Println("flowctl: not yet implemented (Vaihe 0 skeleton)")
}
