# Go build artifacts
/bin/
*.exe
*.test
*.out

# Local env / secrets — never commit
.env
.env.local
*.credentials.json

# Editor / OS
.DS_Store
.idea/
.vscode/

# Postgres data (when running compose locally)
deploy/pgdata/

# Local deploy smoke-test override + orchestrator runtime work dir
deploy/docker-compose.override.yml
deploy/work/

# /refresh command artifacts (personal, local-only)
.claude/refresh-dev.log
.claude/refresh.json

# >>> /run-issues orchestrator runtime state (managed — do not edit) >>>
# Per-run artefacts (logs, prompts, JSON state) and git worktrees created
# by the /run-issues orchestrator. Auto-maintained — never version-controlled.
.claude/run-issues/
.claude/run-issues-archive/
.claude/worktrees/
# <<< /run-issues orchestrator runtime state <<<
